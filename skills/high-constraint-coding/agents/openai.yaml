interface:
  display_name: "High-Constraint Coding"
  short_description: "Minimal-diff, verified, production-quality code changes"
  default_prompt: "Use $high-constraint-coding to make the smallest complete, project-native change: inspect the affected behavior path, keep contracts and local conventions coherent across applicable layers, remove speculative or unreachable code, and verify the behavior with proportionate tests."

policy:
  allow_implicit_invocation: true
