interface:
  display_name: "High-Constraint Coding"
  short_description: "Verified code designed for direct human maintenance"
  default_prompt: "Use $high-constraint-coding to make the smallest complete, project-native change that a human maintainer can locate, trace, hand-edit, and verify; keep ownership, contracts, dependencies, and tests coherent, and avoid speculative architecture or hidden control flow."

policy:
  allow_implicit_invocation: true
