interface:
  display_name: "High-Constraint Coding"
  short_description: "Smallest complete, verified, maintainable code changes"
  default_prompt: "Use $high-constraint-coding to make the smallest complete project-native change. Route only the applicable quality, maintainability, dependency, or protocol references; keep ownership, contracts, failure paths, and tests coherent; remove unnecessary code and report only verified claims."

policy:
  allow_implicit_invocation: true
