interface:
  display_name: "High-Constraint Coding"
  short_description: "Smallest complete, verified, maintainable code changes"
  default_prompt: "Use $high-constraint-coding for the requested implementation, review, or diagnosis. Load only applicable references, produce the smallest complete project-native result, verify every required gate, and report Completed, Implemented but unverified, or Blocked without overstating evidence."

policy:
  allow_implicit_invocation: true
