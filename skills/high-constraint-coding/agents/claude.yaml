interface:
  display_name: "High-Constraint Coding"
  short_description: "Smallest complete, verified, maintainable code changes"
  default_prompt: "Apply the high-constraint-coding skill to make the smallest complete project-native change. Load only the applicable quality, maintainability, dependency, or protocol references; keep ownership, contracts, failure paths, and behavior-focused tests coherent; remove unnecessary code and report only verified claims."

policy:
  allow_implicit_invocation: true
