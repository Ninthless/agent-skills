Context: C++20 Windows library. This is a code excerpt, not a configured CMake project. The subsystem uses `unique_handle`, `expected<T, Error>`, clang-tidy, ASan, and ABI-stable public headers.

```cpp
expected<Session, Error> parse_session(std::string_view input) {
    HANDLE handle = open_session();
    if (handle == INVALID_HANDLE_VALUE) return unexpected(last_error());
    auto header = parse_header(input);
    if (!header) return unexpected(header.error());
    Session session = decode(handle, *header);
    CloseHandle(handle);
    return session;
}
```

`decode` may throw and another parsing branch returns before CloseHandle. Required contract: adopt the established internal RAII owner for all exits while preserving public ABI and expected-based errors. Expected project checks, only when compiler/build configuration exists: focused unit tests, configured clang-tidy, ASan leak paths, and ABI check.
