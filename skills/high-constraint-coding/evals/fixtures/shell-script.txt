Context: Bash deployment script. This is a script excerpt, not an executable deployment repository. RELEASE_DIR and artifact names may contain spaces. Repository conventions use `set -Eeuo pipefail`, arrays, quoted expansions, traps, ShellCheck, and Bats failure fixtures.

```bash
#!/usr/bin/env bash
for file in $(ls $RELEASE_DIR); do
  tar -rf bundle.tar $RELEASE_DIR/$file
  upload bundle.tar
done
rm -rf $RELEASE_DIR/staging
```

Required contract: preserve path boundaries, propagate archive/upload failures, and make staging cleanup conditional and trap-owned so diagnostic evidence is not destroyed on failure. Expected project checks, only when ShellCheck/Bats and project fixtures exist: ShellCheck plus cases for spaces, archive failure, upload failure, and cleanup/recovery.
