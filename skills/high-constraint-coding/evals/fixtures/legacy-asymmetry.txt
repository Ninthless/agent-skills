Context: Kotlin billing subsystem. This is a code excerpt, not a Gradle project. Card gateways support partial reversal; invoice adjustments are append-only ledger entries. Both paths are stable and separately tested.

```kotlin
class CardRefundService(private val policy: RefundPolicy, private val gateway: CardGateway) {
    fun refund(payment: Payment, amount: Money) =
        policy.validate(payment, amount).flatMap { gateway.reverse(payment, amount) }
}
class InvoiceAdjustmentService(private val ledger: AdjustmentLedger) {
    fun adjust(invoiceId: InvoiceId, amount: Money) = ledger.append(Adjustment(invoiceId, amount))
}
class AdjustmentPolicy
class AdjustmentPolicyFactory
interface AdjustmentGateway
```

The proposed classes have no consumer beyond visual symmetry. Required decision: preserve domain-backed asymmetry unless a current behavior or consumer justifies another layer. Review-only contract: produce findings/design guidance without mutation. Exact project checks should be named only if Gradle/build evidence exists; otherwise mark them unexecuted.
