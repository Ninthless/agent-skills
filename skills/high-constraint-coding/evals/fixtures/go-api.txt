Context: Go 1.24 HTTP package. This is a code excerpt, not a complete Go module. Store implementations wrap ErrNotFound; consumers own narrow interfaces. Existing project tooling includes errors.Is assertions, goleak, go vet, and race tests.

```go
var ErrNotFound = errors.New("order not found")
type Store interface { Load(ctx context.Context, id string) (*Order, error) }
type Loader interface { Load(id string) *Order }
func (s *SQLStore) LoadOrder(_ context.Context, id string) *Order {
    order, err := s.Load(context.Background(), id)
    if err != nil { slog.Error("load order", "id", id, "error", err); return nil }
    return order
}
```

Required contract: preserve caller context, retain errors.Is-compatible wrapping and useful context, and avoid an implementation-owned pass-through interface. Expected project checks, only when go.mod and tooling exist: focused tests, `go vet ./...`, `go test -race ./...`, and configured goleak coverage.
