interface:
  display_name: "Build User-Facing UI"
  short_description: "Build distinctive, usable, evidence-checked interfaces"
  default_prompt: "Use $build-user-facing-ui to choose the right process mode; for broad UI refactors, extract product contracts, visually quarantine the old UI, design the destination from a blank canvas, replace the complete layout and UI system, migrate valid functionality, verify every surface, and do not visually derive the replacement from the old UI."

policy:
  allow_implicit_invocation: true
