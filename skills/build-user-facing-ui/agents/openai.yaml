interface:
  display_name: "Build User-Facing UI"
  short_description: "Build distinctive, usable, evidence-checked interfaces"
  default_prompt: "Use $build-user-facing-ui to design, implement, and verify a context-appropriate interface with complete states, platform fit, accessibility, trust, performance evidence, and no fixed visual formula."

policy:
  allow_implicit_invocation: true
