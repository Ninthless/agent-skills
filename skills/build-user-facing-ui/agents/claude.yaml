interface:
  display_name: "Build User-Facing UI"
  short_description: "Build distinctive, usable, evidence-checked interfaces"
  default_prompt: "Apply the build-user-facing-ui skill: understand the real user and task, choose a product-appropriate archetype and visual language, implement complete responsive and accessible states with platform fit and honest choices, then verify the rendered result, performance, and evidence in the target runtime without imposing a fixed style."

policy:
  allow_implicit_invocation: true
