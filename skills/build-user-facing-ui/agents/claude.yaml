interface:
  display_name: "Build User-Facing UI"
  short_description: "Build polished, usable interfaces for any product"
  default_prompt: "Apply the build-user-facing-ui skill: understand the real user and task, choose a product-appropriate interface archetype and visual language, implement the complete interaction with responsive and accessible states, and verify the rendered result in the target runtime. Do not impose a generic AI aesthetic or a fixed visual style."

policy:
  allow_implicit_invocation: true
