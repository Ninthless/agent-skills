interface:
  display_name: "Build User-Facing UI"
  short_description: "Build distinctive, usable, evidence-checked interfaces"
  default_prompt: "Apply the build-user-facing-ui skill: choose the appropriate process mode; interpret broad UI refactor or rebuild requests as Transformation mode, inventory and replace the complete user-facing system from a new product-specific art direction, preserve valid functionality, and verify every migrated surface without an AI-default style."

policy:
  allow_implicit_invocation: true
