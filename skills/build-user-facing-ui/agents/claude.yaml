interface:
  display_name: "Build User-Facing UI"
  short_description: "Build distinctive, usable, evidence-checked interfaces"
  default_prompt: "Apply the build-user-facing-ui skill: choose the appropriate process mode, understand the real user and task, gather references when needed, derive a product-specific art direction, implement complete responsive and accessible states with platform fit, then inspect and refine the rendered result without imposing an AI-default style."

policy:
  allow_implicit_invocation: true
