interface:
  display_name: "Web Search First"
  short_description: "Search for current evidence before acting"
  default_prompt: "Use $websearch-first to search authoritative sources before acting, reconcile external evidence with local facts, and cite the sources that materially affect the result."

policy:
  allow_implicit_invocation: true
