interface:
  display_name: "Web Search First"
  short_description: "Search for current evidence before acting"
  default_prompt: "Apply websearch-first before doing anything: run a web search first, reconcile it with local and user-provided facts, and report the sources that materially support the work."

policy:
  allow_implicit_invocation: true
