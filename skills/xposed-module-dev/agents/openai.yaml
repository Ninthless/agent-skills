interface:
  display_name: "Xposed Module Dev"
  short_description: "Build, review, and debug Android Xposed/LSPosed modules"
  default_prompt: "Use $xposed-module-dev to build, review, or debug this Android Xposed/LSPosed module correctly."

policy:
  allow_implicit_invocation: true
