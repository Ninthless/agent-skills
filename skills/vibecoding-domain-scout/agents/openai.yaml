interface:
  display_name: "Vibecoding Domain Scout"
  short_description: "Research unfamiliar domains before building"
  default_prompt: "Use $vibecoding-domain-scout to research this unfamiliar domain, verify current facts, and turn the idea into a domain map, MVP scope, risks, and build-ready spec."
