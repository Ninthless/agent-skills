interface:
  display_name: "Bug Diagnosis"
  short_description: "Find evidence-backed root causes read-only"
  default_prompt: "Use $bug-diagnosis for unknown-root-cause software investigation. Default to read-only work, build falsifiable hypotheses, run safe discriminating experiments, state confidence and unproven items, and hand fix direction plus verification cases to high-constraint-coding. Do not edit without authorization, guess causes, or develop exploits."

policy:
  allow_implicit_invocation: true
