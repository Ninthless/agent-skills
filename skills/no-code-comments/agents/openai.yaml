interface:
  display_name: "No Code Comments"
  short_description: "Default companion for comment-free code output"
  default_prompt: "Use $no-code-comments whenever this task writes, edits, refactors, scaffolds, regenerates, or shows code. Keep code comment-free unless I explicitly request comments or tooling requires a directive."

policy:
  allow_implicit_invocation: true
