interface:
  display_name: "Git Checkpoint Push"
  short_description: "Safe Git/GitHub checkpoint commit and push"
  default_prompt: "Use $git-checkpoint-push to create a professional Git/GitHub checkpoint commit and push it safely when a completed milestone is ready."

policy:
  allow_implicit_invocation: false
