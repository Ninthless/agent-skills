interface:
  display_name: "Git Checkpoint Push"
  short_description: "检测远端仓库并在阶段性工作完成后自动规范提交并推送"
  default_prompt: "Use -checkpoint-push to create a professional checkpoint commit and push it when a completed milestone is ready."
